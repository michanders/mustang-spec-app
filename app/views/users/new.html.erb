<%= form_for :user, url: '/users' do |f| %>

  Username: <%= f.text_field :username, :autocomplete => :off %>
  Password: <%= f.password_field :password, :autocomplete => :off %>
  Password Confirmation: <%= f.password_field :password_confirmation, :autocomplete => :off %>
  <%= f.submit "Submit" %>

<% end %>
